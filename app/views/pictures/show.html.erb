<h3>投稿詳細</h3>
<%= @picture.title %>
<%= @picture.content %>
<%= image_tag(@picture.image) %>
<br />
<%= link_to "修正", edit_picture_path(@picture.id) %>
<%= link_to "削除", picture_path(@picture.id), method: 'delete', data: {confirm: "本当に削除していいですか？"} %>